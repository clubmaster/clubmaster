== scheduled for beta7 ==
- special products
- test all combinations of subscriptions:
- obey rules of club_user_currency (danish: 1.00, us: 5.5)
- add event managers to events, they should be able to see all events in the club
- make sure we can tax shipping and payment
- explain product_attributes on product page
- add created_at to subscription
- group and user filter, is_active (true, false, both)
- problem with default location on new install
- add class active to right menu
- event calendar view
- event agenda view
== FIXTURE CHANGE ==
changed, account_default_income = 3
added, account_default_cash = 1
added, account_default_discount = 4
added, new account

== scheduled for beta8 ==
- on bought new membership recalc groups for user
- find a way we can reuse numbers (users without subscription with number above X), optional, (set member_number high after 2 years without subscription, or dont have a username without an active membership)
- delete users
- merge users
- find out the syntax for member_numbers
- Update basket

== todo ==
- unit test (has to wait for phpunit 3.5.x)
- add images to products
- sort user lists
- make a quick search field for users
- custom signup fields (register with subscription on signup)
- add category priority ( this will help pick right account_number for products )
- add 365 date icons
- different login pages for different locations
- make infinive parent loop in location configs
- fix layout, single and double column
- configurable emails sent on different events
- improve the way to recalc users of group
- translation
- users should be able to add and remove address, email and phone
  - this will not work with our currently validation.. consider to remove this funcationality from the admin part
- find a way to make company users
- fix product variants
- flag if we run backend script for tasks (to avoid long wait periods for the users)

== rewrite ==
- success / failure handler has to be more dynamic (might wait for a symfony upgrade) (symfony 2.1)
- on success handler, consider forward and not redirect response?
- remove all names from route, and use defaults

== graphics ==
- make sure html are validated
- make sure css are validated

== modules ==
- mailer bundle
  - send mails to specific user
  - send mails to specific group
  - send mail to user filters
- team bundle
- forum bundle
- payment gateway bundle
- weather bundle
- sms bundle
- booking bundle
- integration for e-conomic
- dashboard bundle
  - widgets
    - open orders
    - new members
- export / import bundle
- ranking bundle
- cup bundle
- work schedule bundle
- todo bundle
- wordpress bundle
- api bundle
- request member bundle
- internal message bundle
- statistic bundle
- whoisonline bundle
