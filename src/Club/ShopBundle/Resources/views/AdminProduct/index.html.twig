{% extends "::base.html.twig" %}

{% block body %}

<h1>Product</h1>
<p class="box"><a href="{{ path('admin_shop_product_new') }}" class="btn-create"><span>Create product</span></a></p>

{% include "::flash.html.twig" %}

<form method="post" action="{{ path('admin_shop_product_batch') }}">
<table>
  <thead>
    <tr>
      <th>[x]</th>
      <th>Id</th>
      <th>Product</th>
      <th>Actions</th>
    </tr>
  </thead>
  <tfoot>
    <tr class="bg">
      <td colspan="4" class="arrow-01">
        <select name="" class="input-text">
          <option value="">Delete selected</option>
          <option value="">Remove selected</option>
          <option value="">Rename selected</option>
          <option value="">Copy selected</option>
        </select>&nbsp;<input type="submit" value="OK" />
      </td>
    </tr>
  </tfoot>
  <tbody>
  {% for product in products %}
    <tr>
      <td><input type="checkbox" /></td>
      <td>{{ product.id }}</td>
      <td>{{ product.productName }}</td>
      <td>
        {% include "::edit_link.html.twig" with {'url':path('admin_shop_product_edit', { 'id':product.id })} %}
        {% include "::delete_link.html.twig" with {'url':path('admin_shop_product_delete', { 'id':product.id })} %}
      </td>
    </tr>
  {% else %}
  <tr>
    <td colspan="4">There are no products at the time</td>
  </tr>
  {% endfor %}
  </tbody>
</table>
</form>
{% endblock %}
