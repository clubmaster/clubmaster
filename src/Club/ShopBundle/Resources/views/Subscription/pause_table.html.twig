<h2>Pause</h2>

{% set pause_left = subscription.getStartPauses %}
<table class="style">
  <thead>
    <tr>
      <th>Date</th>
      <th>Return date</th>
      <th>Remaining pause</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>{{ subscription.createdAt|date('Y-m-d H:i') }}</td>
      <td>{{ subscription.createdAt|date('Y-m-d H:i') }}</td>
      <td>{{ pause_left }}</td>
    </tr>
    {% for log in subscription.subscriptionPauses %}
    {% set pause_left = pause_left-1 %}
    <tr>
      <td>{{ log.startDate|date('Y-m-d H:i') }}</td>
      <td>{% if log.expireDate == '' %}-{% else %}{{ log.expireDate|date('Y-m-d H:i') }}{% endif %}</td>
      <td>{{ pause_left }}</td>
    </tr>
    {% endfor %}
  </tbody>
  <tfoot>
    <tr>
      <th colspan="3">&#160;</th>
    </tr>
  </tfoot>
</table>


