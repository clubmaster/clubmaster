{% extends "ClubLayoutBundle:Default:base.html.twig" %}

{% block header %}<h1>Language</h1>{% endblock %}

{% block body %}

<p class="box"><a href="{{ path('admin_language_new') }}" class="btn-create"><span>Create language</span></a></p>

<form method="post" action="{{ path('admin_language_batch') }}">
<table>
  <thead>
    <tr>
      <th>[x]</th>
      <th>Id</th>
      <th>Language</th>
      <th>Actions</th>
    </tr>
  </thead>
  <tfoot>
    <tr class="bg">
      <td colspan="4" class="arrow-01">
        <select name="" class="input-text">
          <option value="">Delete selected</option>
          <option value="">Remove selected</option>
          <option value="">Rename selected</option>
          <option value="">Copy selected</option>
        </select>&nbsp;<input type="submit" value="OK" />
      </td>
    </tr>
  </tfoot>
  <tbody>
  {% for language in languages %}
    <tr>
      <td><input type="checkbox" /></td>
      <td>{{ language.id }}</td>
      <td>{{ language.name }}</td>
      <td>
        {% include "::edit_link.html.twig" with {'url':path('admin_language_edit', { 'id':language.id })} %}
        {% include "::delete_link.html.twig" with {'url':path('admin_language_delete', { 'id':language.id })} %}
      </td>
    </tr>
  {% else %}
  <tr>
    <td colspan="4">There are no languages at the time</td>
  </tr>
  {% endfor %}
  </tbody>
</table>
</form>
{% endblock %}
