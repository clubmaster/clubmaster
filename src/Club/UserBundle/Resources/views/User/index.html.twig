{% extends "ClubUserBundle::base.html.twig" %}

{% block body %}

<p class="box">
  <a href="{{ path('club_user_usersetting_index') }}" class="btn-info"><span>{% trans %}Settings{% endtrans %}</span></a>
</p>

<form method="post" action="{{ path('user') }}">
{% include "ClubUserBundle:User:form.html.twig" %}
</form>

{% endblock %}
