{% extends "::base.html.twig" %}

{% block header %}<h1>Location</h1>{% endblock %}

{% block body %}

<p class="box"><a href="{{ path('admin_location_new') }}" class="btn-create"><span>Create location</span></a></p>

{% include "::flash.html.twig" %}

<form method="post" action="{{ path('admin_location_batch') }}">
<table>
  <thead>
    <tr>
      <th>[x]</th>
      <th>Id</th>
      <th>Parent</th>
      <th>Location</th>
      <th>Actions</th>
    </tr>
  </thead>
  <tfoot>
    <tr class="bg">
      <td colspan="5" class="arrow-01">
        <select name="" class="input-text">
          <option value="">Delete selected</option>
          <option value="">Remove selected</option>
          <option value="">Rename selected</option>
          <option value="">Copy selected</option>
        </select>&nbsp;<input type="submit" value="OK" />
      </td>
    </tr>
  </tfoot>
  <tbody>
  {% for location in locations %}
    <tr>
      <td><input type="checkbox" /></td>
      <td>{{ location.id }}</td>
      <td>{{ location.location }}</td>
      <td>{{ location.locationName }}</td>
      <td>
        {% include "::edit_link.html.twig" with {'url':path('admin_location_edit', { 'id':location.id })} %}
        {% include "::delete_link.html.twig" with {'url':path('admin_location_delete', { 'id':location.id })} %}
        <a href="{{ path('admin_location_config', {'id':location.id}) }}">Config</a>
      </td>
    </tr>
  {% else %}
  <tr>
    <td colspan="5">There are no locations at the time</td>
  </tr>
  {% endfor %}
  </tbody>
</table>
</form>
{% endblock %}
