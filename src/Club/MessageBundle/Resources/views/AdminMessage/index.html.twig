{% extends "ClubMessageBundle::base.html.twig" %}

{% block body %}
<p class="box">
  <a href="{{ path('club_message_adminmessage_new') }}" class="btn-create"><span>{% trans %}Create message{% endtrans %}</span></a>
</p>

<table>
  <thead>
    <tr>
      <th>{% trans %}#{% endtrans %}</th>
      <th>{% trans %}Created at{% endtrans %}</th>
      <th>{% trans %}Subject{% endtrans %}</th>
      <th>{% trans %}Sent at{% endtrans %}</th>
      <th>{% trans %}Status{% endtrans %}</th>
      <th>{% trans %}Action{% endtrans %}</th>
    </tr>
  </thead>
  <tbody>
  {% for message in messages %}
    <tr>
      <td>{{ message.id }}</td>
      <td>{{ message.createdAt|date('Y-m-d H:i:s') }}</td>
      <td>{{ message.subject }}</td>
      <td>
        {% if message.sentAt %}
        {{ message.sentAt|date('Y-m-d H:i:s') }}
        {% endif %}
      </td>
      <td>
        {% if message.processed %}
          {% trans %}Sent{% endtrans %}
        {% else %}
          {% trans %}Queued{% endtrans %}
        {% endif %}
      <td>
        {% if not message.processed %}
        {% include "ClubLayoutBundle:Default:process_link.html.twig" with {'url':path('club_message_adminmessage_process', {'id':message.id})} %}
        {% endif %}
      </td>
    </tr>
  {% else %}
    <tr>
      <td colspan="6">{% trans %}No messages has been sent.{% endtrans %}</td>
    </tr>
  {% endfor %}
  </tbody>
</table>

{% endblock %}
