- only view users own filters
- make a wizard to add recipients
- copy a message for resend
- use jquery autocomplete for user recipient
- add wysiwyg editor to message
- save sent mail report
- make a backend job process messages
- remove unsent mail
- view report of sent mail
- add pager on messages

snippet for sending mails

    try {
      $this->get('clubmaster_mailer')
        ->setSubject($message->getSubject())
        ->setFrom($message->getSenderAddress(),$message->getSenderName())
        ->setTo($user->getProfile()->getProfileEmail()->getEmailAddress())
        ->setBody($message->getMessage())
        ->send();
    } catch (\Exception $e) {
      $error = $e->getMessage();
    }

