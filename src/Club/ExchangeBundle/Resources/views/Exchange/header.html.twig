{% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
<div class="navbar navbar-default" role="navigation">
    <div class="container-fluid">
        <ul class="nav navbar-nav">
            <li><a href="{{ path('club_exchange_exchange_new') }}">{% trans %}Create request{% endtrans %}</a></li>
            {% if exchange is defined and not exchange.closed %}
            <li><a href="{{ path('club_exchange_exchange_edit', {'id':exchange.id}) }}">{% trans %}Edit{% endtrans %}</a></li>
            <li><a href="{{ path('club_exchange_comment_index', {'id':exchange.id}) }}">{% trans %}Comments{% endtrans %}</a></li>
            {% if exchange.user == app.user %}
            <li><a onclick="return confirm('{% trans %}Are you sure?{% endtrans %}');" href="{{ path('club_exchange_comment_close', {'id':exchange.id}) }}">{% trans %}Close{% endtrans %}</a></li>
            {% endif %}
            {% endif %}
        </ul>
    </div>
</div>
{% endif %}
