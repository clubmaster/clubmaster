services:
  club.mailbundle.listener.on_password_reset_listener:
    class: Club\MailBundle\Listener\PasswordResetListener
    arguments: [@doctrine.orm.entity_manager, @mailer, @templating]
    tags:
      - { name: kernel.listener, event: password.reset, method: onPasswordReset }
