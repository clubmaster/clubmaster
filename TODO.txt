== before next release ==
- have the same query builder to users, make a helper that we can use in the repository class
- create unique (subscription_attribute; subscription_id, attribute_name)
- make sure email and phone is NOT mandatory
- fix location dropdown to a prettier solution
- add images to products
- rewrite default settings for language and accounts
- build better forms for filter
- sort user lists
- cannot have multiple is_cancelled or is_accepted status orders
- add batch to user list
- add pager to logging
- add pager to ledger
- add pager to order
- add search to order
- event custom validator ( more attend than max attends )
- make a task that check if a task has been running for more than 15 minutes
- style product page
- style product listing
- put images on links
- fix layout, single and double column
- doctrine migrations
- installer
- what will happen if no shipping, vat, payment_methods or

== todo ==
- remember to set a subscription to inactive when no more tickets
- improve the way to recalc users of group
- translation
- Obey lifetime membership
- make a quick search field for users
- users should be able to disable types of email
  - types (password,info)
- users should be able to add and remove address, email and phone
  - this will not work with our currently validation.. consider to remove this funcationality from the admin part
- find a way to make company users
- Update basket
- fix product variants
- Fix shop as user
- Shop coupons
- database email delivery strategy
- unit test
- special products
- group and user filter, is_active (true, false, both)
- add event managers to events, they should be able to see all events in the club
- make sure tasks can by build with a dynamic event class
- find a way the bundles can add menu items to the menus
  - menu, and priority

== task ==
- flag if we run backend script for tasks (to avoid long wait periods for the users)

== rewrite stuff ==
- rewrite all \Club\UserBundle\Entity\User to ClubUserBundle:User
- success / failure handler has to be more dynamic (might wait for a symfony upgrade) (symfony 2.1)
- on success handler, consider forward and not redirect response?
- remove all names from route, and use defaults
- rewrite prePersist to PrePersist?? Will this work?
- rewrite form.factory to $this->createForm()
- create an address interface for Order/Cart/ProfileAddress

== schema ==

== graphics ==
- event calendar view
- event agenda view
- make sure all pages are html validated
- make sure css are validated
- if twig has no date, it prints current timestamp (tasks last run at)

== security ==
- cannot edit orders that you do not own
- make sure the firewall actually does work
- only right user can pause and expire subscription (admin, owner)

== modules ==
- mailer bundle
  - send mails to specific user
  - send mails to specific group
  - send mail to user filters
- team bundle
- forum bundle
- payment gateway bundle
- weather bundle
- sms bundle
- booking bundle
- integration til e-conomic
- dashboard bundle
  - open orders
- export / import bundle
- ranking bundle
- cup bundle
- work schedule bundle
- todo bundle
- wordpress bundle
- api bundle
- request member bundle
- internal message bundle
- statistic bundle
- whoisonline bundle
